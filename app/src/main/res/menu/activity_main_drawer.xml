<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/mor_cal"
            android:icon="@drawable/ic_menu_camera"
            android:title="Mortage Calculator" />

        <item
            android:id="@+id/price_sqft"
            android:icon="@drawable/ic_menu_slideshow"
            android:title="Price/sqft" />
    </group>

    <item android:title="Investment Calculator">
        <menu>

            <item
                android:id="@+id/rol_cal"
                android:icon="@drawable/ic_menu_send"
                android:title="Rol Calculator" />
            <item
                android:id="@+id/ren_yield"
                android:icon="@drawable/ic_menu_send"
                android:title="Rental Yield" />
        </menu>
    </item>

</menu>
